# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'uninet-ios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for uninet-ios

  target 'uninet-iosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'uninet-iosUITests' do
    # Pods for testing
  end

end
